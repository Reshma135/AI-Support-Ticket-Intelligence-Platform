# AI Support Ticket Intelligence Platform

Recovered frontend base for the working student ML web application.

## Run

1. Keep the existing `.env` file in the project root.
2. Run `npm install` if dependencies are not installed.
3. Run `npm run dev`.
4. Run FastAPI separately from the backend files already present in the project.
